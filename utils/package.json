{
  "name": "utils",
  "version": "1.0.0",
  "description": "``` + No need to sync code. + Less code duplication. - Importing required in each project that needs types. ```",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "snowflake-id": "^1.1.0"
  }
}
