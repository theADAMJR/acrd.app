export declare namespace Auth {
    interface Credentials {
        email?: string;
        password: string;
        username: string;
    }
    interface Payload {
        userId: string;
    }
}
